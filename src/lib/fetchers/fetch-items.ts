'use server';
//import { cookies } from "next/headers";
//import { adaptItemsToSchema } from "../adapters/questionAdapter";

// fetches questions from /api/items and displays them one by one with options
//const res = await fetch('/api/items');
//if (!res.ok) {
  //throw new Error('Failed to fetch questions');
//}
//const data = await res.json();
//export const fetched_questions = data.map(adaptItemsToSchema);




//import { auth } from "@clerk/nextjs/server";
//import { getItems } from "../db/items";

//export async function fetchItems() {
  //const { userId } = await auth();
  //if (!userId) throw new Error("Unauthorized");

  //return  getItems();
//}


//export const fetched_questions = await fetchItems();
